apiVersion: v1
kind: Service
metadata:
  name: {{ include "provider-milvus.fullname" . }}
  labels:
    {{- include "provider-milvus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "provider-milvus.selectorLabels" . | nindent 4 }}
